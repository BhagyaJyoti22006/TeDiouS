1. Create a new Repository in Github.

2. Copy the `action.yml` file from current directory to the repo directory inside `<repo>\.github\workflows\`

3. Replace `<roll>` with the correct roll number.

4. Push the Respository to `main`.

5. Paste the Repository URL which would look like `https://github.com/<user>/<repo>`.
