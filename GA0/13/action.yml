name: Test Action

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: <roll>@ds.study.iitm.ac.in
        run: echo "Hello, world!"