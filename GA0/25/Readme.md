1. Open terminal in the directory `q-vercel-latency`.

2. Run following command:  
`vercel --prod`

3. Follow the instructions to publish the project. Choose a name like `<roll>-q-vercel-latency`. 

4. Copy the `Aliased` URL which would be like `https://<roll>-q-vercel-latency.vercel.app`.

