1. Open terminal in the directory `q-local-llm-structured-server`.

2. Run following command:  
`vercel --prod`

3. Follow the instructions to publish the project. Choose a name like `<roll>-q-local-llm-structured-server`. 

4. Copy the `Aliased` URL which would be like `https://<roll>-q-local-llm-structured-s.vercel.app`.

