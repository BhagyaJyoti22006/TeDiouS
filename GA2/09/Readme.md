1. Open `q-api-idempotency-pagination-server/index.py` and replace `<ORDERS>` and `<LIMIT>` with the assigned values.

2. Open terminal in the directory `q-api-idempotency-pagination-server`.

3. Run following command:  
`vercel --prod`

4. Follow the instructions to publish the project. Choose a name like `<roll>-q-api-idempotency-pagination-server`. 

5. Copy the `Aliased` URL which would be like `https://<roll>-q-api-idempotency-pagina.vercel.app`.

