1. Open terminal in the directory `q-spec-driven-correction-server`.

2. Run following command:  
`vercel --prod`

3. Follow the instructions to publish the project. Choose a name like `<roll>-q-spec-driven-correction-server`. 

4. Copy the `Aliased` URL which would be like `https://<roll>-q-spec-driven-correction.vercel.app/charge`.

